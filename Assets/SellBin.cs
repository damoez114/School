using UnityEngine;
using UnityEngine.EventSystems;

// Drop target for Roe. Matches the same detection pattern HotbarSlot uses —
// Roe.OnEndDrag checks eventData.pointerEnter for this component.
public class SellBin : MonoBehaviour
{
}
